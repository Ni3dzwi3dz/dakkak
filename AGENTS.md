# Agents guide

## Rules
** Docker first ** - whole app, as well as tests and other commands should be run in docker
** Abstractions ** - use clear hierarchy of abstract classes and traits, compose larger objects from smaller
** Clear separation ** - parts of code responsible for different tasks should be clearly separated, also common abstractions should be held separatesy
** Small entities ** - use small files, prefer smaller functions, ensuring declarative style of coding